# A13 - The seam carries the driving map

| Field | Record |
| --- | --- |
| Batch | `A13` |
| Date | 2026-09-09 |
| Name | The seam carries the driving map |
| Status | Closed append-only batch - reference carry; no mod code |
| Threads | [`T-001`](THREADS.md#t-001), [`T-002`](THREADS.md#t-002) |

## Record

`[A12]` wrote the seam's bidirectional goal with one clause dated to
its moment: the sister has not mapped driving yet. The sister's
`[C82]` - no player at the wheel - merged the same day, and the
clause went stale in the same turn it landed. The same-turn rule the
architecture table ratified says a charter that moves is carried the
same turn, so the carry closes here: the canonical paragraph in
`MUTATION.md` now says what the sister's map holds, and nothing else
moves.

## What the sister's map holds

At the sister's `F-067`, disassembled method by method off the
installed jar: every identity gate along the driving path exists to
exclude the blocked local player, and none requires one. The doorway
is seat 0 via `enter(seat, char)`, `isKeyboardControlled()` an
identity test an NPC answers false, the public control fields
standing as written, and an engine start bounded by the same keys,
hotwire, sandbox and condition rules a player faces; the physics
tick holds no driver-identity gate at all. No shipped code exercises
the doorway, so the live receipt stays owed - surface-mapped, not
proven-live.

## What the paragraph now says

The direction stands: read the sister's action machinery, stripped of
what humanity gave it, copying no files. The sister has mapped
driving, so what the crossed need from driving rides in the record as
named consumers of that map rather than feeding forward into an
unmapped half. Explosives and loudspeakers remain unmapped, so the
seam still runs two ways in time for them, and neither side invents
its half alone - a standing goal between the two repositories, never
a dependency in either direction.

## No new border

A reference carry into an already-classified canonical document. The
tree still has no mod code. The sister's `PROJECTS.md` already
carries `[C82]` in its seam section, so that side of the same turn is
done; the standing table's row for this repository moves with this
batch's merge, in the sister's repository, as the rule requires.

## What is still the operator's

Unchanged by this batch: strain names, how many mutations sit on the
gradient and what each is, every number behind every dial, how an
afflicted settlement is held once one forms, the claim surface's
concrete shape (F-012). The fork ledger keeps them.
