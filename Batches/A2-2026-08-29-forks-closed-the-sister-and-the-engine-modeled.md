# A2 - Operator forks closed; the sister and the engine modeled

| Field | Record |
|---|---|
| Batch | `A2` |
| Date | 2026-08-29 |
| Name | Operator forks closed; the sister and the engine modeled |
| Status | Closed append-only batch - play verification pending (no play receipt) |
| Threads | [`T-001`](THREADS.md#t-001), [`T-002`](THREADS.md#t-002) |

## Record

The operator redirected before further ZAO determinations: model SAO and the
game first. This batch is that ground, plus the forks the operator closed on
the way into it.

Three decisions ratified from operator answers: ZAO is standalone and an
integrated sister, both — read SAO's record where present, derive where
absent, one record shape (DR-010); the county's ambient dead carry derived
thin records on SAO's world-origin precedent (DR-011); the player walks a
mutation outcome where the gameplay loop supports it, per outcome, with
NPC-only as the stated default (DR-012). The mutation-axes fork carries the
operator's drafting direction (gradients, branches that are not binaries,
spectrums along branches, decay reversible via mutation, end states past
human strength and speed, hybridization both directions good and bad, no
example names promoted into spec) and stays open in `ROADMAP.md`.

The model landed as two documents. `ENGINE_CONTRACT.md`: the reanimation
path disassembled (timer on the corpse; fresh descriptor; the modData copy;
the DoZombieStats re-roll), the IsoZombie per-body surface, the AI states,
the native and opaque population machinery, the full sandbox zombie-lore
table with defaults, the two infection systems, moodle and event surfaces,
Kahlua's reach, and a nine-item G0 ledger of what stays unverified.
`SAO_SEAM_AUDIT.md`: the sister's record shape and key domains, the one
death funnel and its cache-forget border, the deliberately unowned corpse,
the turning-recognition chain and where F-003 breaks it, five named
collision points (the pool-take deletion first among them), and the dormant
machinery ZAO's settlements will mirror.

Six findings admitted (F-001..F-006), the load-bearing three hand-checked in
this repository's own disassembly pass rather than carried from the sweeps:
the reanimation trigger (F-001), the corpse-modData identity channel
(F-002), the nameless turn (F-003), the DoZombieStats re-roll (F-004), the
per-body axes in vanilla's own vocabulary (F-005), and the native unloaded
crowd (F-006). F-003's upstream consequence for SAO's B3 chain was reported
out as a separate task rather than absorbed here.

No mod code. The interrelation draft for Knox, decay, and mutation remains
in the conversation awaiting operator ratification; nothing of it was
inscribed. `A3` is the next batch.
